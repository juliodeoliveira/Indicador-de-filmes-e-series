#Variable declaration

   #Lists to be compared:
   #user = list(str(input("Qual nome deseja pesquisar? ")).upper())
   

   #user = list(item)
   #divide = []
   #result = []

   #Split all the names
   #for n in lista:
      #split = list(n)
      #divide.append(split)

   #Compare lists (BETA)
   #for l in user:
      #for name in lista:
         #if l == name:
            # Compare if there is already a letter
            #if name not in result:
               #result.append(name)

   # Put the result in a str variable
   #everything = "".join(result)
   #print("Resultados da busca: ", everything)

   #for t in lista:
      #for d in result:
         #if t == d:
            #print(t, end=" ")